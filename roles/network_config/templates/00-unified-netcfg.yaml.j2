network:
  version: 2
  renderer: networkd
  ethernets:
    {{ management_interface | default('eno1') }}:
      dhcp4: true
      dhcp6: true
    {{ storage_interface | default('eno3') }}:
      dhcp4: false
      dhcp6: false
      addresses:
        - {{ ceph_ip }}/24
      mtu: 9000
