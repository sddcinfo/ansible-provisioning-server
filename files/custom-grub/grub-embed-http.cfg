# Embedded config for custom netboot GRUB
# Bypasses TFTP config fetch entirely -- uses HTTP
# Disables per-MAC/per-IP config file search
set feature_net_search_cfg=n
set net_default_server=10.10.1.1
echo "Custom netboot GRUB loading config via HTTP..."
configfile (http,10.10.1.1)/grub/grub.cfg
